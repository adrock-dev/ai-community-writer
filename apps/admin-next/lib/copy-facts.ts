// 화면 안내멘트가 인용하는 사실 값의 단일 출처.
//
// 왜 있는가: 같은 사실을 화면마다 손으로 적으면 서로 다른 말을 하게 된다. 2026-07-28 인벤토리에서
// 실제로 그랬다 — 블로그 리뷰를 켠 학원 동기화 소요시간이 한 화면에서는 「8~15분」, 다른 화면에서는
// 「10분 이상」, 설정 화면에서는 「14분」이었다. 아홉 군데에 흩어져 있어 하나를 고쳐도 나머지가 남았다.
//
// 여기 둘 것: 코드에서 계산할 수 없는 관측값(소요시간 등)만.
// 여기 두지 말 것: API 나 상수에서 계산할 수 있는 값. 그건 서버에서 내려받아 그대로 렌더하라
//                 — 상수를 복사해 오면 원본이 바뀔 때 여기도 같이 썩는다.
//
// 관련: docs/ui-copy-inventory.md (어떤 안내멘트가 어떤 코드에 매달려 있는지)

/** 학원 동기화 소요시간 — 블로그 리뷰 수집이 꺼진 기본 상태. */
export const ACADEMY_SYNC_DURATION = "1~2분";

/**
 * 블로그 리뷰까지 받을 때의 학원 동기화 소요시간.
 * 원천이 동시 요청을 못 견뎌 한 곳씩 받아야 해서 길어진다.
 * 관측값이라 코드에서 계산되지 않는다 — 동시성이나 대상 학원 수가 바뀌면 여기를 고쳐라.
 */
export const ACADEMY_SYNC_DURATION_WITH_BLOG = "10~15분";

/**
 * 「글 작성」 자동 선별 규칙을 사람 말로 옮긴 문장. 버튼 툴팁·추천 안내가 이 하나를 읽는다.
 *
 * 정본은 `apps/api-nest/src/db.service.ts` 의 `selectSlotsForBatch` 다 — **선별 코드를 고치면
 * 이 문장도 같이 고쳐라.** 여기 두는 이유는 두 가지다: 세 버튼(1개·10개·100개)이 같은 규칙을
 * 쓰므로 문장이 한 곳에 있어야 하고, 컴포넌트 안 템플릿 리터럴로 두면 안내멘트 인벤토리
 * (`scripts/copy-inventory.mjs` 의 tooltip 추출은 `title="..."` 형태만 본다)에서 빠져
 * 코드와 어긋나도 아무도 모른다.
 */
export const WRITE_BATCH_RULE =
  "작성 대기(planned)만 · 위 목록의 상태·유형·검색 필터 반영 · 글유형마다 최소 1건 · 나머지는 지역을 골고루 · 같은 지역+키워드는 1건";
