# Post-edit quality

기존 T01 사실 gate(FAQ variant 강제 제외), post-edit fact diff, review lock, neutral evaluator를 함께 적용했다. 익산의 첫 시도는 인근 지역 disclosure와 review quote counting 문제로 제외했고, 새 `runs/iksan-rerun` artifact만 최종 smoke 근거로 사용했다.
