{
  "name": "adrock-content-ops",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "./dev.sh",
    "typecheck": "npm --prefix apps/api-nest run typecheck && npm --prefix apps/admin-next run typecheck",
    "build": "npm --prefix apps/api-nest run build && npm --prefix apps/admin-next run build",
    "audit:all": "npm --prefix apps/api-nest audit --audit-level=high && npm --prefix apps/admin-next audit --audit-level=high",
    "qa:posts": "node scripts/qa-posts.mjs",
    "qa:posts:all": "node scripts/qa-posts.mjs --all",
    "worker:once": "npm --prefix apps/api-nest run worker:once",
    "verify:company-clean": "node scripts/verify-company-clean.mjs",
    "test": "npm --prefix apps/api-nest run test",
    "lint": "biome lint",
    "format": "biome format --write",
    "lint:fix": "biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.1"
  }
}
