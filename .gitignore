# === 의존성 / 빌드 산출물 ===
node_modules/
.pnp.cjs
.pnp.loader.mjs
.yarn/
yarn.lock
out/
dist/
build/
*.tsbuildinfo

# === Python ===
.venv/
.venv_dedup/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# === 런타임 상태 / DB ===
*.db
*.db-wal
*.db-shm
.omc/
.omx/

# === 대용량 데이터 (경쟁사 분석 원본 — git 부적합) ===
*.xlsx
data/*.csv
seed_matrix/04_seed_matrix_full.csv

# === 생성 산출물 ===
output/
data/admin.db*
# 장애 대응용 DB 스냅샷. 위 data/admin.db* 는 하위 경로를 못 걸러 커밋 후보로 올라오고,
# DB 바이너리라 금칙어 검사(verify:company-clean)까지 실패시킨다.
data/incident-*/
# 학원 심층조사 DB(별도 · 초기화 금지 · *.db 로도 이미 제외되지만 명시)
data/academy_research.db*
data/generated-images/
# 생성 실험 산출물(프롬프트 덤프·LLM 원문·메트릭). 재현 가능하고 수십 MB로 불어난다.
# 결론은 docs/content-generation/ 아래 문서로 남기고, 원자료는 로컬에만 둔다.
data/content-generation-evaluation/

# === 비밀/환경 ===
.env
*.local

# === OS / 에디터 ===
.DS_Store
*.log
.idea/
.vscode/

# Next.js
.next/
.playwright-mcp/
